apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: cif
  labels:
    app: cif
    tier: backend
spec:
  selector:
    app: cif
    tier: backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
