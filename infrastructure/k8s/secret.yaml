apiVersion: v1
kind: Secret
metadata:
  name: cif-secrets
  namespace: cif
type: Opaque
# Valeurs d'exemple pour le dev/declaratif. Remplacer par des secrets reels
# en production (kubectl create secret / SealedSecret / Vault).
#   base64: echo -n "votre-secret" | base64
stringData:
  jwt-secret: change-me-in-production
  postgres-password: cif
