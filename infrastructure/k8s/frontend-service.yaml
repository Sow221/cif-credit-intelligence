apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: cif
  labels:
    app: cif
    tier: frontend
spec:
  selector:
    app: cif
    tier: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
