# Donnees d'entrainement suivies par DVC (contenu en cache DVC, pas dans Git).
# Remplir ce repertoire avec le jeu et lancer : dvc add mlops/data/training.csv
# puis dvc push -r s3-remote (config remote a completer avec les credentials).
